type: "http"
method: "post"
inputs:
  - remap_fortiweb
encoding:
  codec: "json"
framing:
  method: "newline_delimited"
uri: "${QUICKWIT_ENDPOINT:-http://localhost:7280}/api/v1/logs-fortinet.fortiweb.{{fwb.type}}/ingest"
